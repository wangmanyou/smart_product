知识库系统需求更新
一、应增加的功能
1，目录创建，要能够批量导入目录（知识的导入可以自动分类到目录——如果批量错误，给反馈）
2，各级目录都要能够调整次序（包括新建知识时的显示）
3，知识应增加“打标签”功能；导入时按照顿号或逗号隔开（标签应该是多个）关键词搜索
4，已打开的页面可以以标签页的形式展示，当前全部回退效率极低（业务中的知识管理时）
5，知识编辑详情页，要增加返回目录的功能，现在除非后退，否则回不去，列表页
6，知识库的展现当前太难进入，应在左侧或哪里能够直接快速点进去，或直接显示知识目录
7，“内容”字段要支持富文本编辑，有些文字需要加粗、红色强调、下划线等（字段类型可以定义富文本框？）
8，知识支持含附件（或除附件外）的全部字段数据的批量导入（待定）
9，知识详情页要d有操作按钮：返回、修改、删除、上一条、下一条等
10，访问日志功能：各账号的登录、退出、操作记录，各知识的更新记录/历史版本 （知识的更新记录）
11，UI易用性要优化
12，编辑过程中不小心点击返回后，再前进时，已经填的内容应该还在编辑框内
13，知识编辑时，自动将“更新时间”保存在写入数据库的时间（更新时间逻辑交给系统不是用户）

二、可能存在问题
1，编辑“场景”时，选择“多个”，没啥效果？如“标签”如何打？
2，我当前的“标签”无法改为“非必填”？（待定）
3，批量导入知识时，只能导入第一行数据？不能把其他字段做到模板里？（查看是否已解决）
4，批量导入时，更新的时间识别错误（系统自己定义）


组织到场景：
场景下有多个知识，知识有分类，现在系统支持无限级别分类，知识的属性可以自定义

角色：
普通用户：只能看到知识，管理员确定用户看到指定场景，可以创建知识。
管理员：除现有功能外，添加普通用户可以看到的场景列表。