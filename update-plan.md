# 知识库系统三期升级计划

## 一、总体升级思路

本次升级不建议一开始就堆新增功能，而是按照使用优先级分三期推进：

1. 第一期：先解决现有系统“不好用”的问题。
2. 第二期：再解决知识维护、导入、检索效率不足的问题。
3. 第三期：最后补充权限、日志、历史版本等管理和审计能力。

这样安排的原因是：当前系统首先影响甲方使用的是整体 UI 结构、入口层级、编辑过程数据丢失、页面跳转不顺等基础体验问题。只有先让系统顺畅可用，后续新增功能才有实际价值。

## 二、第一期：系统 UI 与基础体验重构

### 目标

优先解决整个系统不好用、入口深、页面结构不顺、编辑过程容易丢失数据的问题。

本阶段重点不是新增大量业务功能，而是把现有系统的使用体验调整到可稳定交付、可日常使用的状态。

### 主要内容

#### 1. 整体系统 UI 结构重构

对整个系统的信息架构、导航结构、页面布局和操作入口进行统一优化，而不是只调整某一个业务列表页面。

当前系统偏后台管理风格，普通业务人员进入知识库路径较深，使用时容易找不到核心功能。第一期需要重新梳理系统一级菜单、二级菜单和常用操作入口。

#### 2. 页面布局由左右结构调整为上下结构

根据甲方反馈，当前左右结构使用体验不佳。系统核心业务页面应整体调整为上下结构。

建议方向：

- 上方展示业务场景、目录、筛选条件、搜索框、操作按钮等。
- 下方展示知识列表、详情内容或编辑区域。
- 详情页上方保留返回、修改、删除、上一条、下一条等操作。
- 内容区域下沉，提升阅读和编辑体验。

这里的调整是系统级 UI 规范，不是只针对知识库单个页面。

#### 3. 知识库入口前置

当前知识库入口隐藏较深，需要从“业务管理”下钻后才能进入，甲方认为使用效率低。

建议将知识库相关能力作为更高优先级入口展示，例如：

- 知识中心
- 知识库管理
- 业务知识库

同时不建议完全取消“管理”概念，因为系统仍然需要体现可配置、可维护、可管理的属性。

推荐方案：

- 一级入口使用“知识中心”或“知识库管理”。
- 其下再组织业务场景、知识内容、分类目录等功能。
- 普通用户看到的是更直接的知识入口。
- 管理员仍能感知这是一个可管理的知识库系统。

#### 4. 增加页面 Tab 机制

新增、编辑、详情、列表等页面需要支持以 Tab 页形式打开。

目标是解决当前页面跳转和回退效率低的问题，尤其是在知识新增、知识编辑、知识详情之间切换时，不应反复丢失上下文。

典型效果：

- 打开知识详情时生成一个 Tab。
- 打开新增知识或编辑知识时生成一个 Tab。
- 切回列表页时，原列表筛选条件、分页位置仍然保留。
- 编辑过程中切换 Tab，再切回来，已填写内容仍在。

#### 5. 新增/编辑页面状态保留

编辑知识或新增知识时，如果用户误点返回、切换页面、前进后退，已填写内容不应直接丢失。

这一项属于刚需，因为它直接影响知识录入能否稳定完成。

可考虑的实现方式：

- 前端路由缓存。
- Tab 页状态缓存。
- 表单草稿暂存。
- 离开未保存页面时弹出提醒。

#### 6. 详情页操作体验优化

知识详情页需要补充常用操作按钮，包括：

- 返回
- 修改
- 删除
- 上一条
- 下一条

避免用户只能依赖浏览器后退，也方便在知识列表和知识详情之间连续浏览。

#### 7. 更新时间由系统自动维护

知识新增或修改时，更新时间应由系统自动写入数据库，而不是由用户手动填写。

导入知识时也不应依赖 Excel 中的更新时间字段识别结果，避免出现时间识别错误。

#### 8. 修复现有批量导入问题

第一期需要优先修复已有导入问题，而不是直接扩展复杂导入能力。

包括：

- 批量导入知识时不能只导入第一行。
- 导入模板应包含应有字段。
- 更新时间字段由系统维护。
- 导入错误需要有基础反馈。

#### 9. 修复现有字段配置问题

包括：

- 编辑“场景”时选择“多个”后效果不明显的问题。
- 标签等字段是否可以从必填调整为非必填的问题。
- 字段配置和前端展示效果不一致的问题。

### 第一期交付重点

第一期的核心目标是让甲方感觉系统“终于顺手了”。

关键词：

- 入口更清晰
- 页面更顺
- 编辑不丢数据
- 返回不混乱
- 操作更直接
- 现有导入问题修复

## 三、第二期：知识维护与检索效率提升

### 目标

在第一期解决可用性问题后，第二期重点提升知识维护效率、批量导入效率和知识检索效率。

本阶段才开始处理新增功能和效率型能力。

### 主要内容

#### 1. 知识批量导入时自动匹配目录

这里不是“目录批量导入”。

真实需求是：批量导入知识时，Excel 中包含目录、分类或路径字段，系统根据这些字段自动将知识匹配到对应目录下。

示例：

- Excel 中填写“一级目录/二级目录/三级目录”。
- 系统根据目录路径查找已有目录。
- 匹配成功后，知识自动归类到该目录。
- 匹配失败时，返回具体失败行和失败原因。

#### 2. 导入失败反馈增强

导入失败时不能只提示“导入失败”，而应明确告诉用户：

- 第几行失败。
- 哪个字段失败。
- 失败原因是什么。
- 是否可以继续导入其他正确数据。

常见失败原因包括：

- 目录不存在。
- 分类路径不匹配。
- 字段格式错误。
- 标签格式错误。
- 必填字段为空。

#### 3. 各级目录支持排序

各级目录都需要支持排序。

排序结果应影响：

- 知识库展示。
- 新增知识时目录选择顺序。
- 编辑知识时目录选择顺序。
- 导入匹配时目录展示和选择逻辑。

#### 4. 标签功能

知识需要支持多个标签。

导入时，标签字段应支持通过逗号或顿号拆分。

示例：

- `安全,操作规范,设备维护`
- `安全、操作规范、设备维护`

系统应将其识别为多个标签。

#### 5. 关键词搜索

增加关键词搜索能力，提升知识查找效率。

搜索范围可包括：

- 知识标题
- 知识内容
- 标签
- 自定义字段
- 所属目录

#### 6. 富文本编辑

内容字段需要支持富文本能力，例如：

- 加粗
- 红色强调
- 下划线
- 基础排版

如果第一期只处理编辑稳定性和页面结构，富文本可以放在第二期作为增强功能实现。

### 第二期交付重点

第二期的核心目标是让知识维护和查找更高效。

关键词：

- 导入自动归类
- 失败反馈清晰
- 目录可排序
- 标签可检索
- 搜索更方便
- 内容表达更丰富

## 四、第三期：权限、日志与历史追踪能力

### 目标

在系统可用性和知识维护效率得到改善后，第三期补充管理、权限、审计和追踪能力。

这部分能力重要，但不应抢在基础体验问题之前做。

### 主要内容

#### 1. 权限设计

管理员可以配置普通用户可见的业务场景或知识范围。

普通用户不应看到完整后台管理能力，只能看到自己被授权的知识内容。

#### 2. 普通用户模式

普通用户进入系统后，应以查看和维护知识为主。

普通用户能力建议包括：

- 查看授权场景下的知识。
- 搜索授权范围内的知识。
- 创建知识。
- 根据配置决定是否允许修改或删除。

#### 3. 管理员授权配置

管理员在现有功能基础上，增加普通用户可见场景配置能力。

管理员能力包括：

- 用户管理。
- 场景管理。
- 用户可见场景配置。
- 知识管理。
- 系统配置。

#### 4. 访问日志

记录用户访问和操作行为。

包括：

- 登录记录。
- 退出记录。
- 查看知识记录。
- 新增、修改、删除记录。
- 关键配置变更记录。

#### 5. 知识更新记录

记录知识每次更新的信息。

包括：

- 操作人。
- 更新时间。
- 更新字段。
- 更新前后内容摘要。

#### 6. 历史版本

支持查看知识历史版本。

必要时可以支持版本对比或版本回溯。

#### 7. 附件批量导入

知识附件或包含附件的完整字段批量导入实现复杂度较高，建议放在第三期或单独评估。

需要进一步确认：

- Excel 如何关联附件。
- 附件文件如何打包。
- 附件路径如何识别。
- 导入失败如何回滚。
- 大文件如何处理。

### 第三期交付重点

第三期的核心目标是让系统可管理、可追踪、可审计。

关键词：

- 权限可控
- 用户分层
- 操作留痕
- 知识可追溯
- 历史可查看
- 附件能力增强

## 五、三期计划汇总

| 阶段 | 主题 | 核心目标 | 关键词 |
| --- | --- | --- | --- |
| 第一期 | 系统 UI 与基础体验重构 | 先解决不好用 | 入口、布局、Tab、编辑不丢数据、返回、导入修复 |
| 第二期 | 知识维护与检索效率提升 | 再解决新增功能和效率问题 | 自动匹配目录、导入反馈、目录排序、标签、搜索、富文本 |
| 第三期 | 权限、日志与历史追踪能力 | 最后解决管理和审计问题 | 权限、普通用户、日志、更新记录、历史版本、附件导入 |

## 六、建议对甲方的表达方式

本次升级建议分三步走：

第一期先解决系统不好用的问题，包括整体 UI 结构、入口层级、上下布局、页面 Tab、编辑不丢数据、详情页操作和现有导入问题修复。

第二期再补知识维护效率能力，包括导入知识时自动匹配目录、导入失败反馈、目录排序、标签、关键词搜索和富文本编辑。

第三期最后做管理增强，包括用户权限、可见场景配置、访问日志、知识更新记录、历史版本和附件批量导入。

这样安排可以保证甲方最先感知到系统变好用，同时避免一开始投入大量复杂功能却没有解决日常使用痛点。
