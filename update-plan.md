# 知识库系统三期升级计划

## 一、总体规划概览

结合目前知识库系统的实际使用情况，以及本次沟通中反馈的问题，计划将系统升级分为三个阶段推进。

本次升级的优先级不是先集中增加新功能，而是先解决现有系统在日常使用中的体验问题。当前最影响使用效率的问题主要集中在系统入口层级较深、整体页面结构不够直观、编辑过程中容易丢失已填写内容、页面跳转和返回效率较低等方面。

因此，本次升级整体分为三个阶段：

| 阶段 | 主题 | 核心目标 | 重点内容 |
| --- | --- | --- | --- |
| 第一期 | 系统 UI 与基础体验重构 | 先解决不好用 | 系统入口、上下布局、Tab 页、编辑不丢数据、详情操作 |
| 第二期 | 知识维护与检索效率提升 | 再解决效率和新增功能 | 导入知识自动匹配目录、导入反馈、目录排序、标签、搜索、富文本 |
| 第三期 | 权限、日志与历史追踪能力 | 最后完善管理和审计能力 | 权限配置、普通用户模式、访问日志、更新记录、历史版本 |

### 阶段安排说明

第一期优先处理系统 UI、入口层级、页面结构、Tab 页和编辑不丢数据，确保系统日常使用更加稳定顺畅。

第二期在系统基础体验优化后，再处理知识导入自动归类、导入失败反馈、目录排序、标签、关键词搜索和富文本编辑，提升知识维护和查询效率。

第三期最后完善权限、访问日志、知识更新记录和历史版本等能力，使系统具备更完整的管理和追踪能力。

通过以上安排，可以让系统先解决使用体验问题，再逐步增强业务功能和管理能力，保证升级过程更稳妥，也便于分阶段验收。

## 二、第一期：系统 UI 与基础体验重构

### 目标

第一期重点解决现有系统使用不顺畅的问题，让系统入口更清晰、页面结构更合理、编辑过程更稳定、常用操作更直接。

本阶段的重点不是新增大量复杂功能，而是先把现有系统调整到更适合日常使用的状态。

### 主要建设内容

#### 1. 整体系统 UI 结构重构

本次 UI 调整不是单独针对某一个页面，而是对整个系统的信息架构、导航结构、页面布局和操作入口进行统一优化。

当前系统整体偏后台管理风格，知识库入口较深，用户需要经过多层菜单才能进入核心功能。第一期将重新梳理系统一级菜单、二级菜单和常用功能入口，让知识库相关功能更加直观。

#### 2. 页面布局由左右结构调整为上下结构

系统核心业务页面计划由当前偏左右分栏的结构，调整为更适合知识浏览、筛选和编辑的上下结构。

调整方向包括：

- 页面上方集中展示业务场景、目录分类、搜索条件、筛选项和操作按钮。
- 页面下方展示知识列表、知识详情或知识编辑内容。
- 知识详情页上方保留返回、修改、删除、上一条、下一条等常用操作。
- 内容区域更加突出，提升知识阅读和编辑体验。

该调整属于系统级交互规范，不是只调整某一个业务列表页面。

#### 3. 知识库入口前置

现有知识库入口层级较深，需要先进入“业务管理”等菜单后再进入知识库页面，日常使用不够直接。

第一期只做入口前置，不新增新的业务概念，也不改变原有知识库功能。

调整后，知识库入口直接放到一级菜单或更明显的位置，用户进入系统后可以直接进入知识库列表。

原来知识库中的业务场景、知识列表、知识新增、知识编辑、知识详情等功能保持不变，只是减少进入层级。

调整重点：

- 将知识库入口从原来的多级菜单中前置出来。
- 用户不需要再通过“业务管理”逐级进入。
- 保留原有知识库页面和操作逻辑。
- 优先解决入口太深、进入效率低的问题。

#### 4. 增加页面 Tab 机制

新增、编辑、详情、列表等页面计划支持以 Tab 页形式打开。

该能力主要用于解决当前页面跳转和返回效率较低的问题，尤其是在知识列表、知识详情、新增知识、编辑知识之间频繁切换时，减少重复进入页面和上下文丢失。

预期效果包括：

- 打开知识详情时生成对应 Tab。
- 打开新增知识或编辑知识时生成对应 Tab。
- 切回列表页时，原有筛选条件、分页位置仍然保留。
- 编辑过程中切换到其他页面后，再切回时已填写内容仍然保留。

#### 5. 新增和编辑页面状态保留

知识新增或编辑过程中，如果用户误点返回、切换页面、前进后退，已填写内容不应直接丢失。

该问题会直接影响知识录入稳定性，因此作为第一期重点处理内容。

实现方向可包括：

- 前端页面缓存。
- Tab 页状态缓存。
- 表单草稿暂存。
- 离开未保存页面时增加提醒。

#### 6. 知识详情页操作体验优化

知识详情页计划补充常用操作按钮，减少用户依赖浏览器后退。

包括：

- 返回
- 修改
- 删除
- 上一条
- 下一条

通过这些操作，可以提升知识列表和知识详情之间的连续浏览效率。

#### 7. 更新时间由系统自动维护

知识新增或修改时，更新时间由系统自动写入数据库，不再由用户手动填写。

#### 8. 修复现有字段配置问题

对当前字段配置中已经暴露的问题进行修复。

包括：

- 编辑“场景”时选择“多个”后效果不明显的问题。
- 标签等字段是否可以从必填调整为非必填的问题。
- 字段配置与前端展示效果不一致的问题。

### 第一期交付重点

第一期的目标是让系统先变得顺手、稳定、易进入。

交付重点包括：

- 系统入口更清晰。
- 页面结构更符合使用习惯。
- 新增和编辑过程不易丢失数据。
- 列表、详情、编辑之间切换更顺畅。
- 常用操作更加直接。
- 现有字段配置问题得到修复。

## 三、第二期：知识维护与检索效率提升

### 目标

第二期在第一期体验优化的基础上，重点提升知识维护效率、批量导入效率和知识检索效率。

本阶段开始补充新增功能和效率型能力。

### 主要建设内容

#### 1. 批量导入知识时自动匹配目录

本项需求不是“批量导入目录”，而是指批量导入知识时，系统能够根据 Excel 中填写的目录、分类或路径字段，自动将知识匹配到已有目录下。

示例：

- Excel 中填写“一级目录/二级目录/三级目录”。
- 系统根据目录路径查找已有目录。
- 匹配成功后，知识自动归类到对应目录。
- 匹配失败时，返回具体失败行和失败原因。

#### 2. 导入失败反馈增强

导入失败时，系统需要提供更明确的反馈，而不是只提示“导入失败”。

反馈内容包括：

- 第几行失败。
- 哪个字段失败。
- 失败原因是什么。
- 是否可以继续导入其他正确数据。

常见失败原因包括：

- 目录不存在。
- 分类路径不匹配。
- 字段格式错误。
- 标签格式错误。
- 必填字段为空。

#### 3. 各级目录支持排序

各级目录需要支持排序，排序结果应在系统相关位置保持一致。

影响范围包括：

- 知识库展示顺序。
- 新增知识时目录选择顺序。
- 编辑知识时目录选择顺序。
- 目录相关筛选和展示顺序。

#### 4. 标签功能

知识支持多个标签，用于提升知识分类和检索能力。

批量导入时，标签字段支持通过逗号或顿号拆分。

示例：

- `安全,操作规范,设备维护`
- `安全、操作规范、设备维护`

系统应将其识别为多个标签。

#### 5. 关键词搜索

增加关键词搜索能力，提升知识查找效率。

搜索范围可包括：

- 知识标题
- 知识内容
- 标签
- 自定义字段
- 所属目录

#### 6. 富文本编辑

知识内容字段支持基础富文本能力，满足内容强调和排版需求。

包括：

- 加粗
- 红色强调
- 下划线
- 基础排版

### 第二期交付重点

第二期的目标是提升知识维护和知识检索效率。

交付重点包括：

- 导入知识时可自动归类。
- 导入错误反馈更清晰。
- 目录顺序可维护。
- 标签可用于管理和检索。
- 搜索能力更完善。
- 知识内容表达更丰富。

## 四、第三期：权限、日志与历史追踪能力

### 目标

第三期在系统体验和知识维护效率改善后，补充权限控制、操作日志、知识更新记录和历史版本能力。

该阶段主要解决系统管理、审计和追踪问题。

### 主要建设内容

#### 1. 权限设计

管理员可以配置普通用户可见的业务场景或知识范围。

普通用户不展示完整后台管理能力，只能查看和维护授权范围内的知识内容。

#### 2. 普通用户模式

普通用户进入系统后，以知识查看和知识维护为主。

普通用户能力可包括：

- 查看授权场景下的知识。
- 搜索授权范围内的知识。
- 创建知识。
- 根据配置决定是否允许修改或删除知识。

#### 3. 管理员授权配置

管理员在现有能力基础上，增加用户可见场景配置能力。

管理员能力包括：

- 用户管理。
- 场景管理。
- 用户可见场景配置。
- 知识管理。
- 系统配置。

#### 4. 访问日志

记录用户访问和操作行为。

包括：

- 登录记录。
- 退出记录。
- 查看知识记录。
- 新增、修改、删除记录。
- 关键配置变更记录。

#### 5. 知识更新记录

记录知识每次更新的信息。

包括：

- 操作人。
- 更新时间。
- 更新字段。
- 更新前后内容摘要。

#### 6. 历史版本

支持查看知识历史版本。

后续可根据实际需求扩展版本对比或版本回溯能力。

### 第三期交付重点

第三期的目标是让系统具备更完善的管理、审计和追踪能力。

交付重点包括：

- 权限范围可配置。
- 普通用户和管理员能力区分清晰。
- 操作行为可记录。
- 知识更新可追溯。
- 历史版本可查看。
